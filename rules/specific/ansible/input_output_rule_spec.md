# Ansible Role Argument Specification Pattern

All roles uses `meta/argument_specs.yml` to specify argument. When creating or updating `meta/argument_specs.yml` files for Ansible roles, follow the pattern specified in `input_output_spec.md`.
